# Ski Star Demo Variant

**Demo only.** Ski Star ski resort variant for the Viaplay demo app.

Edit `Configuration/demo-config.json` and set `"active": "skistar"` to use this variant.

Uses skistar-logo for brand/contest assets. Other images use placeholders (img1) until Ski Star-specific images are added.
